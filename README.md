# Kalmar Union website

SvelteKit project for the Kalmar Union website. Hosted as a Cloudflare worker with automatic deployments from the `main` branch.

To run locally with automatic reload on local changes:

```bash
npm run dev
```
